#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {
    int n;
    scanf("%d", &n);          // Read size of array
    int arr[n];

    // Read array elements
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int value;
    scanf("%d", &value);      // Read value to remove

    int k = 0;  // Counter for elements not equal to value

    // Loop through array
    for(int i = 0; i < n; i++) {
        if(arr[i] != value) {   // Keep only elements not equal to value
            arr[k] = arr[i];    // Move to the front
            k++;
        }
    }

    // Print the first k elements
    for(int i = 0; i < k; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
